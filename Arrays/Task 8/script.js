// -------------------- Task #8 - array functions --------------------------

const colors = ["red", "green", "blue"];
const checkColor = (arr, color) => {
  return arr.indexOf(color);
};
console.log(checkColor(colors, "blue"));
